<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">

    <title>Document</title>
    <style>
        #wrapper {
            width: 400px;
            height: 200px;
            border: 5px solid rgb(126, 102, 180);
            margin: auto;
        }

        input {
            width: 95%;
            height: 45px;
            background: #d5d5d5;
            font-size: 24px;
        }

        div {
            margin-bottom: 5px;
        }
    </style>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script>
        function checkValid() {
            // 아이디 비밀번호 유효성 검증
            var id = document.querySelector("input[name = 'id']");
            var password = document.querySelector("input[name = 'password']");

            if (id.value.length < 1) {
                alert("아이디를 입력하세요.");
                id.focus();
            } else if (password.value.length < 1) {
                alert("비밀번호를 입력하시오.");
                password.focus();
            }

            $("form").attr({
                "action" :"/member/login.jsp",
                "method" : "post"
            });
            $("form").submit();
            

        }
        $(document).ready(function () {

            $("#id").animate({
                left: "0px"
            })
            $("#password").animate({
                left: "0px"
            })

            $("button").click(function () {
                checkValid();
            })
        })

    </script>
</head>

<body>
    <form>
        <div id="wrapper">
            <div style="position: relative; left : -400px;">
                <input type="text" placeholder="input your id..." name="user_id"  id="id">
            </div>
            <div style="position:relative; left: -400px;">
                <input type="password" placeholder="input your password..." name="pass"  id ="password">
            </div>
            <div style="text-align: center;">
                <!-- html5에서 제공되는 button 요소는 내부적으로 자동 submit() 처리되어 있어서
                특히 form 태그로 감샀을 때 개발자가 submit() 호출하지 않아도 자동으로 전송한다.
                방지 방법 : js에서 막는방법, button 요소를 form 태그에서 꺼내버린다. -->
                <button>로그인</button>
            </div>
        </div>
    </form>
</body>

</html>>